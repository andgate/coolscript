// This is a comment
let
  id = (x) => x;
  const = (a, b) => a;
  createFoo = (x) => {
    name: "Foo",
    value: x
  };
  bar = 999;
in do {
  baz = id(bar);
  const(bar, baz);
  return createFoo(baz);
}
