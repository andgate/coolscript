// This is a comment
let
  id = (x) => x;
  constant = (a, b) => a;
  createFoo = (x) => {
    name: "Foo",
    value: x
  };
  bar = 999;
in do {
  baz = id(bar);
  constant(bar, baz);
  return createFoo(baz);
}
