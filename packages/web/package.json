{
  "name": "coolscript-web",
  "private": true,
  "version": "0.0.1",
  "description": "Website for coolscript",
  "source": "src/index.html",
  "scripts": {
    "start": "parcel",
    "build": "parcel build --dist-dir ../../docs --no-source-maps --no-cache --no-content-hash --public-url ./",
    "check": "tsc --noEmit",
    "prettier": "prettier --check src/ tests/",
    "prettier:fix": "prettier --write src/ tests/",
    "lint": "eslint src/** tests/** --ext .ts,.tsx,.html",
    "lint:fix": "pnpm lint --fix",
    "verify": "pnpm prettier && pnpm lint",
    "verify:fix": "pnpm prettier:fix && pnpm lint:fix",
    "test": "jest",
    "clean": "pnpm clean:etc",
    "clean:node_modules": "rimraf node_modules",
    "clean:etc": "rimraf dist coverage .pnpm-debug.log"
  },
  "author": "Gabriel Anderson",
  "license": "MIT",
  "devDependencies": {
    "@coolscript/parser": "^0.0.3",
    "@coolscript/syntax": "^0.0.3",
    "csstips": "^1.2.0",
    "csx": "^10.0.2",
    "react": "^17.0.2",
    "react-dom": "^17.0.2",
    "typestyle": "^2.2.0"
  }
}
