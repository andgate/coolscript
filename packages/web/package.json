{
  "name": "coolscript-web",
  "private": true,
  "version": "0.0.2",
  "description": "Website for coolscript",
  "source": "src/index.html",
  "scripts": {
    "start": "parcel",
    "build": "parcel build --dist-dir ../../docs --no-source-maps --no-cache --no-content-hash --public-url ./",
    "check": "tsc --noEmit",
    "prettier": "prettier --check src/ tests/",
    "prettier:fix": "prettier --write src/ tests/",
    "lint": "eslint src/** tests/** --ext .ts,.tsx,.html",
    "lint:fix": "pnpm lint --fix",
    "verify": "pnpm prettier && pnpm lint",
    "verify:fix": "pnpm prettier:fix && pnpm lint:fix",
    "test": "jest"
  },
  "author": "Gabriel Anderson",
  "license": "MIT",
  "devDependencies": {
    "@coolscript/eval": "^0.0.1",
    "@coolscript/parser": "^0.0.4",
    "@coolscript/syntax": "^0.0.4",
    "csstips": "^1.2.0",
    "csx": "^10.0.2",
    "react": "^17.0.2",
    "react-dom": "^17.0.2",
    "typestyle": "^2.2.0"
  }
}
