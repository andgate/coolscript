{
  "name": "coolscript-platform",
  "private": true,
  "description": "Monorepo for the coolscript language platform.",
  "version": "0.0.0",
  "engines": {
    "pnpm": ">=6",
    "npm": ">=8",
    "node": ">=16"
  },
  "scripts": {
    "start": "pnpm build && lerna run start --parallel",
    "bootstrap": "lerna bootstrap",
    "postinstall": "pnpm bootstrap",
    "prepublish": "pnpm build",
    "check": "lerna run check",
    "verify": "lerna run verify --stream",
    "prettier": "lerna run prettier",
    "build": "pnpm clean:docs && lerna run build && pnpm nojekyll",
    "build:lib": "pnpm build:plugin && lerna run build:lib",
    "build:plugin": "lerna run build:plugin",
    "test": "jest",
    "clean": "pnpm clean:build && pnpm clean:etc && pnpm clean:node_modules",
    "clean:all": "pnpm run clean && rimraf node_modules",
    "clean:docs": "rimraf docs/",
    "clean:build": "rimraf packages/*/dist/ packages/*/lib/ **/*.tsbuildinfo",
    "clean:dist": "rimraf packages/*/dist/ **/*.tsbuildinfo",
    "clean:lib": "rimraf packages/*/lib/ **/*.tsbuildinfo",
    "clean:etc": "rimraf .parcel-cache **/*.tsbuildinfo **/.pnpm-debug.log **/lerna-debug.log **/coverage",
    "clean:node_modules": "rimraf packages/*/node_modules/",
    "nojekyll": "nodetouch docs/.nojekyll"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/andgate/coolscript.git"
  },
  "author": "Gabriel Anderson",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/andgate/coolscript/issues"
  },
  "homepage": "https://andgate.github.io/coolscript",
  "devDependencies": {
    "@types/jest": "^27.4.0",
    "@types/node": "^17.0.10",
    "@typescript-eslint/eslint-plugin": "^5.10.0",
    "@typescript-eslint/parser": "^5.10.0",
    "concurrently": "^7.0.0",
    "esbuild": "^0.14.12",
    "esbuild-loader": "^2.18.0",
    "eslint": "^8.7.0",
    "eslint-config-prettier": "^8.3.0",
    "eslint-plugin-html": "^6.2.0",
    "eslint-plugin-prettier": "^4.0.0",
    "eslint-plugin-react": "^7.28.0",
    "eslint-plugin-react-hooks": "^4.3.0",
    "favicons": "^6.2.2",
    "favicons-webpack-plugin": "^5.0.2",
    "html-webpack-plugin": "^5.5.0",
    "jest": "^27.4.7",
    "lerna": "^4.0.0",
    "npm-dts": "^1.3.10",
    "prettier": "2.5.1",
    "prettier-config-standard": "^4.0.0",
    "rimraf": "^3.0.2",
    "touch": "^3.1.0",
    "ts-jest": "^27.1.3",
    "typescript": "^4.5.5",
    "webpack": "^5.65.0",
    "webpack-cli": "^4.9.1",
    "webpack-dev-server": "^4.7.0"
  }
}
